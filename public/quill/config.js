export const config = {
    // Particle configuration
    particlesCount: 4000,
    particlesRadius: 22,
    particleSize: 0.12,
    
    // Bloom effect configuration
    bloomStrength: 1.2,
    bloomRadius: 0.65,
    bloomThreshold: 0.15,
    
    // Colors (rgb values from 0 to 1)
    accentPrimaryColor: {
        r: 1.0,
        g: 1.0,
        b: 1.0
    },
    accentSecondaryColor: {
        r: 0.95,
        g: 0.95,
        b: 0.95
    },
    
    // Dev mode (set to true to enable dev tools by default)
    devMode: false
};